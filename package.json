{
	"name": "telegram-bot",
	"version": "0.0.0",
	"private": true,
	"engines": {
		"node": ">=22"
	},
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "wrangler dev",
		"start": "wrangler dev",
		"test": "vitest",
		"test:unit": "vitest --config vitest.node.config.mts",
		"test:e2e": "vitest --config vitest.e2e.config.mts",
		"test:e2e:local": "RUN_E2E_LOCAL_WRANGLER=1 vitest --config vitest.e2e.config.mts test/e2e/local-telegram-webhook.spec.ts --run",
		"cf-typegen": "wrangler types --env-file /dev/null"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.16.18",
		"typescript": "^5.9.3",
		"vitest": "^4.1.9",
		"wrangler": "^4.103.0"
	},
	"dependencies": {
		"grammy": "^1.44.0"
	}
}
