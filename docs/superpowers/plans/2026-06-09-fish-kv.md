# Fish KV Catalog Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Move the fish catalog into `FISH_KV` and add paid `/fish add <name> <value>`.

**Architecture:** Add a focused catalog helper for KV persistence and keep fishing probability logic in `fishCore`. Command handlers load the catalog asynchronously before selecting fish.

**Tech Stack:** TypeScript, Cloudflare Workers KV, Vitest.

---

### Task 1: KV Catalog Helper

**Files:**
- Create: `src/lib/fishCatalog.ts`
- Modify: `src/data/fish.ts`
- Test: `test/lib/fishCatalog.spec.ts`

- [ ] Write tests for seed initialization and add behavior.
- [ ] Implement `getFishCatalog`, `addFishToCatalog`, and `getHookRateForValue`.
- [ ] Run `npm test -- test/lib/fishCatalog.spec.ts`.

### Task 2: Command Integration

**Files:**
- Modify: `src/commands/fish.ts`
- Modify: `src/lib/fishCore.ts`
- Test: `test/commands/fish.spec.ts`

- [ ] Write command tests for `/fish add` success, invalid value, and insufficient balance.
- [ ] Make `catchFish` accept the catalog as an argument.
- [ ] Load `FISH_KV` catalog in `/fish`, callback handling, and guarantee logic.
- [ ] Run `npm test -- test/commands/fish.spec.ts`.

### Task 3: Bindings and Docs

**Files:**
- Modify: `src/index.ts`
- Modify: `wrangler.jsonc`
- Modify: `docs/fish-system.md`

- [ ] Create Cloudflare KV namespaces for dev and prod.
- [ ] Add `FISH_KV` binding IDs to `wrangler.jsonc`.
- [ ] Add `FISH_KV` to `Env`.
- [ ] Update fish documentation.
- [ ] Run `npm test`.
